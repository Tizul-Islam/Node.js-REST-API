# Node.js Native REST API (with TypeScript) - Learning Notes

এই প্রজেক্টটি কোনো থার্ড-পার্টি ফ্রেমওয়ার্ক (যেমন Express.js) ব্যবহার না করে, সম্পূর্ণ **Native Node.js (http মডিউল)** এবং **TypeScript** ব্যবহার করে তৈরি করা একটি RESTful API। ডাটাবেস হিসেবে এখানে ফাইল সিস্টেম (`data.json`) ব্যবহার করা হয়েছে। 

নিচে প্রজেক্টের ফোল্ডার স্ট্রাকচার, কাজের ধারা এবং ব্যবহৃত ফাংশন/মেথডগুলোর A to Z বিস্তারিত আলোচনা করা হলো:

---

## ১. ফোল্ডার স্ট্রাকচার এবং কাজের বিবরণ (Folder Structure & Responsibilities)

*   **`src/server.ts`**: এটি অ্যাপ্লিকেশনের এন্ট্রি পয়েন্ট। এখানে `http` মডিউল ব্যবহার করে সার্ভার তৈরি করা হয়েছে এবং নির্দিষ্ট পোর্টে (যেমন: 3000) সার্ভারটিকে রান করানো হয়েছে। সমস্ত ইনকামিং রিকোয়েস্ট প্রথমে এখানে আসে এবং `routerHandler`-এর কাছে পাঠানো হয়।
*   **`src/config/index.ts`**: এনভায়রনমেন্ট ভেরিয়েবল (যেমন: পোর্ট নম্বর) লোড এবং ম্যানেজ করার কাজ এখানে করা হয়েছে। `.env` ফাইল থেকে কনফিগারেশন পড়ার জন্য `dotenv` ব্যবহার করা হয়েছে।
*   **`src/routes/routes.ts`**: রিকোয়েস্টের URL এবং Method (GET, POST, PUT, DELETE) যাচাই করে, ট্রাফিককে সঠিক কন্ট্রোলারের কাছে পাঠানো (Routing) এর কাজ।
*   **`src/controllers/product.controller.ts`**: প্রজেক্টের মূল লজিক (Business Logic) এখানে লেখা হয়েছে। `req` এবং `res` হ্যান্ডেল করে ক্লায়েন্টকে কী রেসপন্স দিতে হবে, তা এই ফাইলে নির্ধারণ করা আছে। প্রোডাক্টের সমস্ত CRUD (Create, Read, Update, Delete) লজিক এখানে রয়েছে।
*   **`src/service/product.service.ts`**: কন্ট্রোলার সরাসরি ডাটাবেসের সাথে কথা বলে না, এই কাজ করে সার্ভিস ফাইল। এখানে `fs` মডিউলের মাধ্যমে `data.json` ফাইল রিড (Read) এবং রাইট (Write) করার লজিক লেখা আছে।
*   **`src/database/data.json`**: এটি আমাদের লোকাল ডাটাবেস। প্রোডাক্টের ডেটা এখানে JSON অ্যারে হিসেবে সেভ করা থাকে।
*   **`src/type/product.type.ts`**: টাইপস্ক্রিপ্টের সাহায্যে ডেটার কাঠামো (Structure) ঠিক করার জন্য এখানে `IProduct` ইন্টারফেস (Interface) তৈরি করা হয়েছে (id, name, price)।
*   **`src/utility/parseBody.ts`**: ক্লায়েন্ট থেকে আসা রিকোয়েস্টের বডি (Data Chunk) রিড করে সেটিকে JSON অবজেক্টে রূপান্তর করার জন্য একটি হেল্পার ফাংশন।
*   **`src/utility/sendRespons.ts`**: বারবার `res.writeHead` এবং `res.end` লেখার বদলে একটি কমন হেল্পার ফাংশন তৈরি করা হয়েছে, যা দিয়ে সহজেই প্রমিত (standardized) রেসপন্স পাঠানো যায়।

---

## ২. ব্যবহৃত কোর মডিউল এবং ফাংশন/মেথডসমূহ (Core Modules & Methods Used)

### ক. Node.js Native Modules:
1.  **`node:http` (HTTP মডিউল):**
    *   `createServer()`: একটি বেসিক HTTP সার্ভার তৈরি করতে।
    *   `IncomingMessage` (req): ক্লায়েন্ট থেকে আসা রিকোয়েস্ট অবজেক্ট। এর প্রপার্টি হিসেবে `req.url` (যেমন `/products`), `req.method` (যেমন `GET`) ব্যবহার করা হয়েছে।
    *   `req.on('data', chunk => ...)`: ক্লায়েন্ট থেকে আসা ডেটার চাঙ্ক গ্রহণ করতে (Stream reading)।
    *   `req.on('end', () => ...)`: রিকোয়েস্টের ডেটা আসা শেষ হলে তা বুঝতে।
    *   `ServerResponse` (res): ক্লায়েন্টকে রেসপন্স পাঠানোর অবজেক্ট।
    *   `res.writeHead(statusCode, headers)`: রেসপন্সের স্ট্যাটাস কোড (যেমন: 200, 404, 201) এবং হেডার (যেমন: `Content-Type: application/json`) সেট করতে।
    *   `res.end(data)`: ক্লায়েন্টকে রেসপন্স পাঠিয়ে কানেকশন ক্লোজ করতে।

2.  **`node:fs` (File System মডিউল):**
    *   `fs.readFileSync(filePath, 'utf-8')`: ডাটাবেস (`data.json`) থেকে সিঙ্ক্রোনাসভাবে ডেটা পড়তে।
    *   `fs.writeFileSync(filePath, data)`: ডাটাবেসে নতুন বা আপডেট করা ডেটা সেভ করতে।

3.  **`node:path` (Path মডিউল):**
    *   `path.join(process.cwd(), ...)`: অপারেটিং সিস্টেম অনুযায়ী সঠিকভাবে ফাইলের পাথ তৈরি করতে (যাতে ম্যাক/উইন্ডোজে পাথ সংক্রান্ত এরর না আসে)।
    *   `path.resolve(...)`: অ্যাবসিউট পাথ বের করতে (`.env` ফাইলের জন্য)।

### খ. JavaScript/TypeScript Core Features:
1.  **Promises (`new Promise`, `resolve`, `reject`):** 
    *   `parseBody.ts`-এ অ্যাসিঙ্ক্রোনাস কাজ (ডেটা স্ট্রিম রিড করা) হ্যান্ডেল করার জন্য Promise ব্যবহার করা হয়েছে, যেন কন্ট্রোলারে `await parseBody(req)` ব্যবহার করা যায়।
2.  **Array Methods:**
    *   `.find()`: নির্দিষ্ট আইডির প্রোডাক্ট খুঁজে বের করতে (`GET Single Product`)।
    *   `.findIndex()`: আপডেট বা ডিলিট করার সময় অ্যারেতে প্রোডাক্টের ইনডেক্স (অবস্থান) বের করতে।
    *   `.push()`: নতুন প্রোডাক্ট অ্যারেতে যুক্ত করতে (`POST`)।
    *   `.splice()`: অ্যারে থেকে কোনো নির্দিষ্ট প্রোডাক্ট মুছে ফেলতে (`DELETE`)।
3.  **JSON Methods:**
    *   `JSON.parse()`: JSON ফরম্যাটের স্ট্রিং ডেটাকে জাভাস্ক্রিপ্ট অবজেক্টে রূপান্তর করতে।
    *   `JSON.stringify()`: অবজেক্টকে JSON স্ট্রিংয়ে রূপান্তর করতে (রেসপন্স পাঠানোর সময় বা ফাইলে সেভ করার সময়)।
4.  **String Methods:**
    *   `.split('/')`: URL-কে ভেঙে ID বের করতে (যেমন `/products/123` -> `['', 'products', '123']`)।
    *   `.startsWith()`: URL নির্দিষ্ট কোনো পাথ দিয়ে শুরু হয়েছে কিনা তা চেক করতে।

### গ. Third-Party Packages:
1.  **`dotenv`**: 
    *   `dotenv.config()`: `.env` ফাইল থেকে কনফিগারেশন ভেরিয়েবলগুলো `process.env` তে লোড করার জন্য।

---

## ৩. প্রজেক্ট ফ্লো (কীভাবে API কাজ করছে?)

1.  **Request Initiation:** ক্লায়েন্ট (যেমন Postman) একটি রিকোয়েস্ট পাঠায় (যেমন `GET /products`)। এটি `src/server.ts` রিসিভ করে।
2.  **Routing:** `server.ts` রিকোয়েস্টটিকে `routes.ts` এর `routerHandler`-এ পাঠায়।
3.  **Controller Action:** `routes.ts` URL-টি চেক করে সেটি `product.controller.ts`-এর কাছে পাঠায়।
4.  **Service Interaction:** কন্ট্রোলার বুঝতে পারে তাকে সব প্রোডাক্ট ফেরত দিতে হবে। সে তখন সরাসরি ডাটাবেসে হাত না দিয়ে `product.service.ts`-এর `readProduct()` কে কল করে।
5.  **Database Read:** `readProduct()` ফাইল সিস্টেম (`fs`) ব্যবহার করে `data.json` থেকে ডেটা পড়ে এনে কন্ট্রোলারকে দেয়।
6.  **Response Generation:** কন্ট্রোলার ডেটা পাওয়ার পর, `sendResponse` ইউটিলিটি ফাংশন ব্যবহার করে রেসপন্সটি (সাকসেস মেসেজ, স্ট্যাটাস কোড এবং ডেটাসহ) ক্লায়েন্টকে পাঠিয়ে দেয়।

---

## ৪. ইন্টারভিউ প্রশ্ন (Interview Questions - Basic to Advanced)

আপনার এই প্রজেক্টের ওপর ভিত্তি করে ইন্টারভিউতে যে ধরনের প্রশ্ন করা হতে পারে, তার একটি তালিকা নিচে দেওয়া হলো:

### 🟢 বেসিক লেভেল (Basic Level)
1.  **Express.js ব্যবহার না করে Native `http` মডিউল দিয়ে সার্ভার তৈরি করার সুবিধা ও অসুবিধা কী?**
    *   *Hint:* সুবিধা হলো কোনো থার্ড-পার্টি ডিপেন্ডেন্সি নেই এবং Node.js-এর কোর মেকানিজম খুব ভালোভাবে শেখা যায়। অসুবিধা হলো রাউটিং এবং মিডলওয়্যার হ্যান্ডেল করা অনেক জটিল এবং সময়সাপেক্ষ।
2.  **`req.url` এবং `req.method` বলতে কী বোঝায়?**
    *   *Hint:* `req.url` ক্লায়েন্ট কোন পাথে রিকোয়েস্ট করেছে তা বোঝায় (যেমন: `/products`), আর `req.method` রিকোয়েস্টের ধরন বোঝায় (যেমন: `GET`, `POST`)।
3.  **`res.writeHead()` এর কাজ কী?**
    *   *Hint:* রেসপন্সের স্ট্যাটাস কোড (২০০, ৪০৪ ইত্যাদি) এবং মেটাডেটা/হেডার (যেমন `Content-Type: application/json`) সেট করার জন্য এটি ব্যবহৃত হয়।
4.  **TypeScript-এ `interface` কেন ব্যবহার করেছেন? `type` এবং `interface`-এর মধ্যে পার্থক্য কী?**
    *   *Hint:* অবজেক্টের স্ট্রাকচার ডিফাইন করার জন্য `interface` ব্যবহার করা হয়। `interface` মূলত অবজেক্টের জন্য কাজ করে এবং একে এক্সটেন্ড (extend) করা সহজ, অন্যদিকে `type` দিয়ে যেকোনো ডেটা টাইপ (যেমন Union, Primitive) ডিফাইন করা যায়।

### 🟡 ইন্টারমিডিয়েট লেভেল (Intermediate Level)
5.  **কন্ট্রোলার (Controller) এবং সার্ভিস (Service) ফাইলগুলোকে কেন আলাদা করেছেন?**
    *   *Hint:* Separation of Concerns (SoC) মেইনটেইন করার জন্য। কন্ট্রোলারের কাজ শুধু রিকোয়েস্ট রিসিভ করা এবং রেসপন্স পাঠানো। আর ডাটাবেস বা বিজনেস লজিকের কাজ সার্ভিসের। এতে কোড রিইউজেবল এবং সহজে টেস্ট (Test) করা যায়।
6.  **`parseBody.ts` ফাইলে `req.on('data')` এবং `req.on('end')` কেন ব্যবহার করা হয়েছে?**
    *   *Hint:* Node.js-এ বড় ডেটা একবারে আসে না, বরং ছোট ছোট চাঙ্ক (chunk) বা স্ট্রিম (stream) আকারে আসে। `data` ইভেন্ট দিয়ে ডেটার টুকরোগুলোকে জোড়া লাগানো হয় এবং `end` ইভেন্ট দিয়ে ডেটা আসা শেষ হলে তাকে পার্স করে অবজেক্টে রূপান্তর করা হয়।
7.  **`fs.readFileSync` এবং `fs.readFile`-এর মধ্যে পার্থক্য কী? আপনি কেন `readFileSync` ব্যবহার করেছেন?**
    *   *Hint:* `readFileSync` হলো সিঙ্ক্রোনাস (Synchronous), যা ডেটা পড়া শেষ না হওয়া পর্যন্ত কোড এক্সিকিউশন আটকে (block) রাখে। আর `readFile` হলো অ্যাসিঙ্ক্রোনাস (Asynchronous)। যেহেতু এটি একটি বেসিক প্রজেক্ট, তাই সহজে ইমপ্লিমেন্ট করার জন্য সিঙ্ক্রোনাস ব্যবহার করা হয়েছে, তবে রিয়েল-ওয়ার্ল্ড অ্যাপে অবশ্যই অ্যাসিঙ্ক্রোনাস ব্যবহার করা উচিত।
8.  **REST API-এর প্রিন্সিপালগুলো কী কী? আপনার API কি RESTful?**
    *   *Hint:* হ্যাঁ, কারণ এটি নির্দিষ্ট এন্ডপয়েন্ট (`/products`), স্ট্যান্ডার্ড HTTP মেথড (GET, POST, PUT, DELETE) এবং JSON ডেটা এক্সচেঞ্জ ফরম্যাট অনুসরণ করে।

### 🔴 অ্যাডভান্সড লেভেল (Advanced Level)
9.  **Node.js সিঙ্গল-থ্রেডেড (Single-threaded) হওয়া সত্ত্বেও কীভাবে কনকারেন্সি (Concurrency) হ্যান্ডেল করে?**
    *   *Hint:* Node.js মূলত **Event Loop** এবং **Non-blocking I/O**-এর মাধ্যমে কনকারেন্সি হ্যান্ডেল করে। অ্যাসিঙ্ক্রোনাস কাজগুলো (যেমন: নেটওয়ার্ক রিকোয়েস্ট, ফাইল রিড) ব্যাকগ্রাউন্ডে (C++ worker threads বা OS kernel-এ) চলে যায় এবং শেষ হলে কলব্যাক বা প্রমিজের মাধ্যমে Event Loop-এ ফিরে আসে।
10. **আপনার প্রজেক্টে `fs.readFileSync` ব্যবহার করা হয়েছে। যদি একই সাথে হাজার হাজার রিকোয়েস্ট আসে, তাহলে আপনার সার্ভারে কী সমস্যা হবে এবং কীভাবে সমাধান করবেন?**
    *   *Hint:* সিঙ্ক্রোনাস মেথড ব্যবহারের কারণে Node.js-এর সিঙ্গল থ্রেড ব্লক হয়ে যাবে এবং অন্যান্য রিকোয়েস্টগুলো প্রসেস হতে পারবে না (সার্ভার স্লো বা হ্যাং হয়ে যাবে)। এর সমাধান হলো `fs.promises.readFile` (অ্যাসিঙ্ক্রোনাস মেথড) ব্যবহার করা অথবা ডেডিকেটেড ডাটাবেস (MongoDB, PostgreSQL) ব্যবহার করা।
11. **Promise কী? `parseBody.ts` ফাইলে আপনি ইভেন্ট লিসেনারের ভেতরে `new Promise` কেন ব্যবহার করেছেন?**
    *   *Hint:* প্রমিজ হলো কোনো একটি অ্যাসিঙ্ক্রোনাস কাজের ভবিষ্যতের ফলাফল (Success বা Error)। `req.on()` ইভেন্ট লিসেনারগুলো অ্যাসিঙ্ক্রোনাস হওয়ায়, কন্ট্রোলারে `await` ব্যবহার করে সম্পূর্ণ বডির ডেটা পাওয়ার জন্য ইভেন্টগুলোকে প্রমিজ দিয়ে র‍্যাপ (wrap) করা হয়েছে।
12. **যদি আপনার `data.json` ফাইলটিতে হঠাৎ করে কয়েক গিগাবাইট (GB) সাইজের ডেটা চলে আসে, তখন আপনার `readProduct` মেথড কীভাবে আচরণ করবে?**
    *   *Hint:* `fs.readFileSync` সম্পূর্ণ ফাইলকে একবারে র‍্যামে (RAM/Memory) লোড করে। ফাইলের সাইজ খুব বড় হলে Node.js-এর মেমরি লিমিট (সাধারণত ১.৫ - ২ GB) পার হয়ে যাবে এবং অ্যাপ্লিকেশন `Memory Heap Out Of Bounds` এরর দিয়ে ক্র্যাশ করবে। এই সমস্যা সমাধানে **Node.js Streams** (`fs.createReadStream`) ব্যবহার করতে হয়।
